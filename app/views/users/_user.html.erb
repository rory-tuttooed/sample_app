<li>
  <%= gravatar_for(user, size: 30) %>
  <%= link_to user.name, user %> via email: <%= user.email %>
  <% if signed_in? && current_user.admin? && !current_user?(user) %>
    | <%= link_to "delete", user, method: :delete, confirm: "You sure?" %>
  <% end %>

</li>

